<h1><%= @category.name %> </h1>
<% @category.users.each do |user| %>

<div class= "user-list">
    <h2><%= user.firstname%><%= user.lastname%> </h2>
    <%= image_tag user.profile_url %>
    <p> user rating  </p>
    
</div>

<% end %>